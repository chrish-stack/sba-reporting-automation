[project]
name = "sba-reporting-automation"
version = "0.0.1"
description = "Local-first SBA reporting assistant pipeline"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
sba-reporting = "sba_reporting.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
