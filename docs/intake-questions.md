# Intake Request for Taylor / JLP

For the Santa Barbara reporting automation proof of concept, please send or grant access to:

1. One clean weekly report example where the process was straightforward.
2. One messy weekly report example with overlapping months, repeated ad names, multiple audiences, or pillar cleanup.
3. The raw platform exports used for each example.
4. The final manually cleaned Google Sheet output for those same weeks.
5. Any GA4 or website-performance file paired with those weeks.
6. A list of approved audience names.
7. A list of known campaign names.
8. A list of known pillars.
9. A list of known objectives.
10. Current weekly report required-column layout, especially the columns that raw exports must match.
11. Access to TapClicks report/API or a walkthrough of exactly where the relevant fields live.
12. Any screen recording showing export to final insight.
13. Notes on which fields are safe to automate versus which fields JLP/Taylor still want to review manually.

## Suggested Slack/email copy

Can you send over a small test package for the Santa Barbara reporting automation?

To avoid trying to automate the whole beast at once, we want to validate one simple case and one messy edge case first. Ideally we need:

- One clean weekly report example.
- One messy weekly report example with overlapping months, repeated ad names, multiple audiences, or pillar cleanup.
- The raw platform exports for those weeks.
- The final manually cleaned Google Sheet output for those same weeks.
- Any GA4/site-performance file that was paired with those weeks.
- The approved audience/campaign/pillar/objective lists if those exist.
- TapClicks access/API details or a quick walkthrough of where the fields live.

The goal is to compare automated cleaned output against the manual final output, then expand once the small case is accurate.
